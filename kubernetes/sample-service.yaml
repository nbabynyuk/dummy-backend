apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  type: LoadBalancer
  selector:
    app: sample-backend
  ports:
    # By default and for convenience, the `targetPort` is set to the same value as the `port` field.
    - port: 30008
      targetPort: 8080